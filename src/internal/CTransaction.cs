// using System;
// using System.Runtime.InteropServices;

namespace Cfd
{
  /*
    internal static class CTransaction
    {
      internal static extern CfdErrorCode CfdInitializeMultisigSign(void* handle, void** multisignHandle);

      internal static extern CfdErrorCode CfdAddMultisigSignData(
          void* handle, void* multisignHandle, const char* signature,
          const char* relatedPubkey);

      internal static extern CfdErrorCode CfdAddMultisigSignDataToDer(
          void* handle, void* multisignHandle, const char* signature,
          int sighashType, bool sighashAnyoneCanPay, const char* relatedPubkey);

      internal static extern CfdErrorCode CfdFreeMultisigSignHandle(void* handle, void* multisignHandle);
    }
  */
}
